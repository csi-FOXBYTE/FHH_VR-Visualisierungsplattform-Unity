using System.Collections.Generic;
using Cysharp.Threading.Tasks;

namespace Foxbyte.Core.Services.UserStateService
{
    public interface IUserStateStorageService
    {
        //UniTask SaveCurrentUserAsync(CurrentUser currentUser);
        //UniTask<CurrentUser> GetCurrentUserAsync();
        //UniTask<User> GetLoggedInUserAsync();
        //UniTask SaveLoggedInUserAsync(User user);
        //UniTask SaveUserPermissionsAsync(List<Permission> permissions);
        //UniTask<List<Permission>> GetUserPermissionsAsync();
        //UniTask ClearUserStateAsync();
    }
}